// Auth middleware — placeholder until Clerk keys are added
// Dashboard is currently open; will be protected once Clerk is wired up
export default function middleware() {}

export const config = { matcher: [] };
