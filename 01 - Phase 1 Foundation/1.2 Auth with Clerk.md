---
title: "1.2 Auth with Clerk"
phase: 1
section: "1.2"
status: review
tags: [phase-1, auth, clerk, nextjs]
created: 2026-06-06
updated: 2026-06-06
week: 1
priority: high
---

# 1.2 Auth with Clerk

> Integrate Clerk for user authentication — Google + Email sign-in, route protection, and user sync to Neon.

---

> [!tip] Status: Built — awaiting Clerk API keys
> Code is complete. `@clerk/nextjs` installed, middleware, sign-in/sign-up pages, user sync all built.
> **Action needed:** Add `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` + `CLERK_SECRET_KEY` to Vercel env vars.

---

## Task Checklist

- [ ] Install and configure `@clerk/nextjs`
- [ ] Add Google + Email sign-in providers in Clerk dashboard
- [ ] Add middleware to protect `/dashboard` routes
- [ ] Sync Clerk user to Neon `users` table on first sign-in
- [ ] Add sign-in / sign-up buttons to navbar

---

## Implementation Notes

### Install
```bash
npm install @clerk/nextjs
```

### middleware.ts
```typescript
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server'

const isProtected = createRouteMatcher(['/dashboard(.*)'])

export default clerkMiddleware((auth, req) => {
  if (isProtected(req)) auth().protect()
})

export const config = { matcher: ['/((?!.*\\..*|_next).*)', '/'] }
```

### Env vars needed
```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard
```

### User sync (Clerk webhook → Neon)
```typescript
// app/api/webhooks/clerk/route.ts
// On user.created event → INSERT INTO users (clerk_id, email, name)
```

---

## Blockers / Open Questions

- [ ] Create Clerk app at clerk.com → get API keys
- [ ] Add Clerk env vars to Vercel project settings

---

## Related

- [[01 - Phase 1 Foundation/1.1 Database Schema]] — `users` table
- [[01 - Phase 1 Foundation/1.3 Search History Dashboard]] — uses auth session
- [[01 - Phase 1 Foundation/Overview]]
